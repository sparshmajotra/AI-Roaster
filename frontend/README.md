# Roastly Frontend

Next.js product UI for Roastly.

```bash
npm install
npm run dev
```

Set `NEXT_PUBLIC_API_URL` to point at the Django API. The current screen includes mock feed data and interactive local generation states so design work can continue before auth wiring is complete.
